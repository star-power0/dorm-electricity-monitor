# Open Source Roadmap

> 记录当前桌面版宿舍电费监控项目的下一阶段规划，避免后续遗忘或重复讨论。
>
> 记录时间：2026-06-30

## 1. 当前项目定位

- 项目类型：Windows 本地桌面工具
- 目标用户：非技术用户、本校学生宿舍用户
- 当前形态：Electron + React + TypeScript 桌面界面，Python 保留核心查询与通知逻辑
- 发布方式：源码运行 + 本地桌面打包，后续可扩展到 GitHub Release
- 生命周期：长期维护工具

## 2. 当前共识

### 2.1 UI 可以继续美化

当前 UI 已经达到“可用、基本不滚动”的状态，但还没有到成熟开源产品的质感。

下一轮美化目标不是改功能，而是做视觉统一和产品化打磨。

建议重点：

1. 字体系统统一
   - 中文标题更稳
   - 正文字重更清楚
   - 数字和金额显示更有节奏

2. 卡片系统统一
   - 圆角统一
   - 阴影统一
   - 卡片内边距统一
   - 标题、说明、操作区节奏统一

3. 按钮系统统一
   - 主按钮、次按钮、幽灵按钮层级更明确
   - 高度、字重、圆角统一

4. 状态颜色统一
   - 正常 / 偏低 / 告急 三套语义色统一映射到卡片、badge、摘要区

5. 页面信息密度再优化
   - 总览页更像仪表盘
   - 通知页更像配置工作台
   - 设置页更像系统面板

### 2.2 开源前应补隐私协议和首次引导

因为项目涉及以下本地敏感信息，开源前应该补齐最小合规说明：

- `openId`
- 邮箱账号
- SMTP 授权码
- 监控状态和本地配置
- 可能暴露宿舍信息

建议增加首次启动引导流程：

1. 欢迎页
   - 工具用途
   - 能做什么
   - 不能做什么

2. 隐私与使用说明
   - 数据默认只保存在本地
   - 需要用户自己提供 `openId`
   - 邮件和 PushPlus 会向用户配置的目标发送提醒
   - 项目与学校官方无直接隶属关系
   - 接口可能失效或变化

3. 首次配置
   - 输入 `openId`
   - 设置通知方式
   - 设置阈值
   - 设置收件人

4. 完成确认
   - 测试查询
   - 测试邮件
   - 保存并进入主界面

建议增加本地记录字段：

- `privacyConsentVersion`
- `privacyConsentedAt`
- `onboardingCompleted`

后续建议补充文档：

- `PRIVACY.md`
- `DISCLAIMER.md`
- `OPEN_SOURCE_NOTICE.md`（或整合进 README 对应章节）

### 2.3 当前主线保持单宿舍

基于现有接口与抓包判断，当前实现应明确保持为“一个 `openId` 查询一个宿舍场景下的两类电表”，不把多宿舍或全校级能力放进当前主路线。

当前事实：

- 查询接口目前只用到 `openId + type`
- `type=1` 表示照明
- `type=2` 表示空调
- 当前配置结构只有一个 `openId`
- 当前 `meters` 只是该宿舍下的两个电表类型
- 小程序侧宿舍绑定是固定场景，不是任意宿舍自由切换

结论：

1. 现在能确认的是：
   - 当前版本适合单宿舍场景
   - 当前最值得投入的是 UI、引导和开源文档打磨

2. 当前不作为主线推进的是：
   - 多宿舍配置版
   - 全校宿舍统一监控
   - 任意宿舍扫描或枚举能力

## 3. 后续扩展的保守态度

如果未来还要继续研究，只能作为独立课题，不纳入当前产品化打磨主线。

保守前提：

- 不默认承诺多宿舍
- 不默认承诺全校扫描
- 不把接口研究结果写成既成事实
- 没有新的明确证据前，README 与产品文案都保持单宿舍定位

如果后续单独重开任务，才再判断：

- 是否值得支持多个用户主动提供的 `openId`
- 是否有必要做多宿舍配置版
- 是否存在新的合规边界风险

## 4. 推荐分阶段路线

### 阶段一：先把当前单宿舍版本做成可开源产品

目标：

- UI 第二轮视觉统一
- 首次引导
- 隐私协议 / 使用说明
- 开源说明文档

完成标准：

- 单宿舍功能不变
- UI 更成熟
- 开源时用户能理解数据、配置和风险边界

### 阶段二：继续补单宿舍版本的开源体验

目标：

- 完善帮助文案
- 优化首次引导细节
- 根据用户反馈继续打磨设置页和通知页说明

完成标准：

- 普通用户能独立完成 `openId`、SMTP、收件人配置
- 隐私与免责声明入口清晰

### 阶段三：如有必要，再单独重开 openId 扩展研究

目标：

- 只在有新事实前提下，重新评估是否支持多个授权 `openId`
- 明确是否值得把“多配置源”做成单独版本

完成标准：

- 给出“继续不做 / 只做多个授权 openId / 暂不建议实现”的明确结论

## 5. 当前推荐执行顺序

建议顺序：

1. 先做 UI 第二轮美化
2. 再做隐私协议 + 首次引导
3. 最后继续补单宿舍配置说明和开源文档

原因：

- 这三项都是确定可落地的产品化工作
- 当前接口事实不支持把“多宿舍/全校扫描”作为主方向

## 6. 验证矩阵（后续实施时使用）

### 基础验证

- [ ] UI 修改后构建通过
- [ ] 桌面端可启动
- [ ] 配置保存正常

### 功能验证

- [ ] 首次引导可完成
- [ ] 同意状态可记录
- [ ] 未同意协议时不能直接进入正式监控界面
- [ ] 首次配置失败时可返回上一步
- [ ] 测试查询和测试邮件仍可用

### 多宿舍验证

- [ ] 多宿舍状态互不污染
- [ ] 通知文案带宿舍上下文
- [ ] 历史状态按宿舍隔离
- [ ] 单宿舍旧配置可兼容升级

### 发布验证

- [ ] README / PRIVACY / DISCLAIMER 齐全
- [ ] 打包产物存在
- [ ] 本地 git 状态干净
- [ ] GitHub Release 说明完整

## 7. 暂定边界

当前暂不做：

- 自动充值
- 云端账号系统
- 远程同步
- 多人协作后台
- 默认承诺“全校宿舍统一监控”
- 在未验证接口前做激进的全校扫描能力

## 8. 一句话结论

当前项目最稳妥的路线是：

**先把单宿舍版本做成可开源的成熟桌面工具，再把多宿舍支持作为下一阶段配置能力，最后再单独研究是否存在全校级接口扩展空间。**
