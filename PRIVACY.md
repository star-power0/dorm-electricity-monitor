# PRIVACY

## 1. 本项目处理哪些数据

当前项目是本地桌面辅助工具，可能会在本机保存以下信息：

- `openId`：用于识别你在小程序中已经绑定的宿舍场景
- 发件邮箱地址
- SMTP 授权码
- 收件人邮箱列表
- 本地监控状态、最近查询日志与提醒状态
- 电表余额、剩余电量、采集时间等查询结果
- 首次引导完成状态与隐私同意版本

这些信息默认保存在当前电脑，不会默认上传到作者服务器。

## 2. 数据默认保存在哪里

开发模式下，配置和状态默认保存在项目目录。

打包后的桌面版首次启动时，会把示例配置复制到应用用户数据目录，后续配置和状态也写在用户数据目录中。

本项目当前没有：

- 云端账号系统
- 作者服务器同步
- 远程配置托管
- 默认上传日志或遥测

## 3. 网络请求范围

当前版本可能发起以下网络请求：

1. 向海南大学相关电费接口发起余额查询请求。
2. 如果启用了邮箱提醒，会连接你填写的 SMTP 服务器发送邮件。
3. 如果启用了 PushPlus，会向 PushPlus 服务发送提醒请求。
4. 如果你点击文档或帮助链接，系统会打开对应网页。

## 4. openId 的用途与来源

`openId` 用来标识你在对应微信小程序中的当前用户身份与绑定场景。

在本项目中，`openId` 只用于查询你本人已经绑定的宿舍电表场景。本项目不会自动枚举其他宿舍，也不会默认提供全校扫描能力。

你通常需要通过你自己授权范围内的调试 / 抓包过程获得 `openId`。例如：使用 Reqable 等工具观察你本人打开小程序电费页面时产生的请求参数。

## 5. SMTP 授权码的用途

如果启用邮箱提醒，本项目会使用你填写的 SMTP 配置发送邮件提醒。

请注意：

- SMTP 授权码不是邮箱登录密码
- 授权码应由你本人在邮箱服务商页面生成
- 不要把授权码公开提交到 Git、Issue、截图或聊天记录中

## 6. 你需要自行承担的配置责任

你需要自行确认：

- `openId` 来源于你自己的合法调试过程
- 发件邮箱和 SMTP 授权码由你本人控制
- 收件人是你愿意发送提醒的目标
- 你理解学校接口、微信小程序和邮箱服务策略都可能变化
- 你不会将本项目用于未经授权的数据查询

## 7. 安全建议

- 不要提交真实 `config.json`
- 不要公开真实 `openId`
- 不要公开 SMTP 授权码、PushPlus token 或邮箱密码
- 开源截图、演示视频和 issue 附件里请打码房间、邮箱、日志和余额
- 如果怀疑授权码泄露，请立即到邮箱服务商后台撤销并重新生成

## 8. 不提供的保证

本项目不保证：

- 学校接口长期稳定可用
- 查询结果永远准确、实时或完整
- 邮件或 PushPlus 提醒一定送达
- 小程序参数和接口路径不会变化

请把它当作一个本地辅助提醒工具，而不是官方计费系统。